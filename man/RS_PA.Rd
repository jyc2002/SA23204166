% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RS_PA.R
\name{RS_PA}
\alias{RS_PA}
\title{Random Signflips parallel analysis using R}
\usage{
RS_PA(X, T, a)
}
\arguments{
\item{X}{the sample matrix}

\item{T}{the number of trials}

\item{a}{the percentile}
}
\value{
k the number of leading singular values above the 𝛼-percentile of their signflipped analogues
}
\description{
Random Signflips parallel analysis using R
}
\examples{
\dontrun{
    mat=matrix(rbinom(50^2,1,0.2),ncol=50)
    upper_triangle <- upper.tri(mat)
    sim_data <- matrix(0, nrow = nrow(mat), ncol = ncol(mat))
    sim_data[upper_triangle] <- mat[upper_triangle]
    sim_data[lower.tri(sim_data)] <- t(sim_data)[lower.tri(sim_data)]
    RS_PA(sim_data,300,0.95)
    data(dolphins)
    RS_PA(dolphins,300,0.95)
    data(football)
    RS_PA(football,300,0.95)
    data(karate)
    RS_PA(karate,300,0.95)
    data(polbooks)
    RS_PA(polbooks,300,0.95)
}
}
